name: platform_handler
description: Organize Flutter MethodChannel calls, native callbacks, and request-id based ping-pong responses.
homepage: https://github.com/leo1394/platform_handler
repository: https://github.com/leo1394/platform_handler
issue_tracker: https://github.com/leo1394/platform_handler/issues
version: 1.1.0

environment:
  sdk: ^3.6.1

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  enhanced_change_notifier: ^0.2.0
  extension_dart: ^1.0.10

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0

topics:
  - platform-channel
  - method-channel
  - native-bridge
  - callbacks
  - request-id
